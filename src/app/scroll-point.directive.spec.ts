import { ScrollPointDirective } from './scroll-point.directive';

describe('ScrollPointDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollPointDirective();
    expect(directive).toBeTruthy();
  });
});
